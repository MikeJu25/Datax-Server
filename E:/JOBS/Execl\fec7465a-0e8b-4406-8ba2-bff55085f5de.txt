


select * from sys_app

type: "application/vnd.ms-excel"

表：
readerData.parameter.connection[0].table[0]

字段
readerData.parameter.column

集合
readerData.parameter.collectionname

writerData.parameter.collectionName

字段：
readerData.parameter.MdColumn[count-1].name


writerData.parameter.MGcolumn[MGcoCount-1].name
